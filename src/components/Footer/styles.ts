import styled from "styled-components";
import { colors } from "../../utils/colors";

export const Footer = styled.footer`
    width: 100%;
    height: 34px;
    background: ${colors.footer[0]};
    text-align: center;

    font-weight: 400;
    font-size: 12px;
    line-height: 15px;
    margin-top: 140px;
`